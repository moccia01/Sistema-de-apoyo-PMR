{{#partial "contenido" }}
    <div class="container mt-4">
        <h1>Comunidades</h1>

        <div class="btn-group">
            <button type="button" class="btn btn-secondary dropdown-toggle" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Filtrar Por Rol
            </button>
            <div class="dropdown-menu">
                <a class="dropdown-item" href="#" id="filterOpen">Administrador</a>
                <a class="dropdown-item" href="#" id="filterClosed">Usuario</a>
            </div>
        </div>

        <!--aca arrancaria la lista de comunidades-->
    {{#each comunidades}}
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">{{this.nombre}}</h5>
                <p class="card-text">Descripcion Comunidad</p>
                <a href="../usuarios/verUsuarios.html" class="btn btn-dark">Ver usuarios</a>
                <a href="../usuarios/administrarUsuarios.html" class="btn btn-secondary">Admin usuarios</a>
            </div>
        </div>
    {{/each}}


{{/partial}}
{{> templates/plantilla_base }}